{{-- user info and avatar --}}
<div class="avatar av-l chatify-d-flex"></div>
<p class="info-name">{{ __('messenger.name_unknown') }}</p>
<div class="messenger-infoView-btns">
    <a href="#" class="danger delete-conversation">{{ __('messenger.delete_conversation') }}</a>
</div>
{{-- shared photos --}}
<div class="messenger-infoView-shared">
    <p class="messenger-title"><span>{{ __('messenger.shared_photos') }}</span></p>
    <div class="shared-photos-list"></div>
</div>
